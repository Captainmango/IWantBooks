<%if current_user.admin%>
<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <table class="table table-striped table-bordered">
            <thead>
                <th>Email</th>
                <th>Comment </th>
                <th>Time </th>
                <th>Admin </th>
            </thead>
            <tbody>
                <% user_comments.each do |comment| %>
                <tr>
                    <td><%=comment.user.email%></td>
                    <td><%=comment.content%></td>
                    <td><%=comment.created_at %></td>
                    <td><%=link_to "Delete", comment_path(comment), method: :delete, data: { confirm: 'Are you certain you want to delete this?' }%> / 
                    <%= link_to "Edit", edit_comment_path(comment) %></td>
                </tr>
                <%end%>
            </tbody>
            </table>
        </div>
    </div>
</div>
<%else%>
<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <table class="table table-striped table-bordered">
            <thead>
                <th>Email</th>
                <th>Comment </th>
                <th>Time </th>
                <th>Admin </th>
            </thead>
            <tbody>
                <% user_comments.each do |comment| %>
                <tr>
                    <td><%=comment.user.email%></td>
                    <td><%=comment.content%></td>
                    <td><%=comment.created_at %></td>
                    <%if current_user.id = comment.user_id%>
                    <td><%=link_to "Delete", comment_path(c), method: :delete, data: { confirm: 'Are you certain you want to delete this?' }%> / 
                    <%= link_to "Edit", edit_comment_path(c) %></td>
                    <%end%>
                </tr>
                <%end%>
            </tbody>
            </table>
        </div>
    </div>
</div>
<%end%>
